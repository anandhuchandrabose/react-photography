import React from 'react'
import ReactDOM from 'react-dom/client'
import Image from './ImageGallery'

ReactDOM.createRoot(document.getElementById('image')).render(
  <React.StrictMode>
    <Image />
  </React.StrictMode>,
)